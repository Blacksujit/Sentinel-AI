session_id: 20260826-risk-trend
task: "Add risk trend charts to the org dashboard — backend trend endpoint + frontend recharts chart + docs"
agent_count: 3
base_branch: main
created: 2026-08-26T00:00:00Z
template: feature-builder

# API CONTRACT — all agents build against this, not each other's code
contract:
  endpoint: "GET /api/orgs/{org_id}/usage/trend"
  query_params:
    days: 30  # default, range 1-365
  response:
    type: array
    items:
      date: "2026-08-01"       # ISO date string (day bucket)
      avg_risk_score: 0.23    # float 0-1, average final_risk_score that day
      event_count: 45         # int, total events that day
      critical_count: 2       # int, events with final_risk_score >= 0.8 that day
